---
interface Props {
  id?: string;
  items: string[];
}
const { id = "key-takeaways", items } = Astro.props;
---

<section class="takeaways" aria-labelledby={id}>
  <h2 id={id}>Key Takeaways</h2>
  <ul>
    {items.map((item) => <li>{item}</li>)}
  </ul>
</section>
