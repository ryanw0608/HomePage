---
title: "Digest 2026-w31"
date: "2026-08-01"
summary: "One course note on gradient descent and one reviewed paper note on the Transformer; no revisits due this week."
model: "glm-5.2"
---

## This week in the notebook

Two notes are published, both dated 2026-07-06:

- **Gradient Descent as an Optimization Baseline** (`ml-foundations`) — records the update rule $\theta_{t+1} = \theta_t - \eta \nabla_\theta J(\theta_t)$, a worked one-step example on $J(w) = (w-3)^2$, and learning-rate failure modes (overshoot/divergence vs. imperceptible progress). Featured, math-enabled.
- **Reading: Attention Is All You Need** (`paper-reading`) — structured notes on Vaswani et al. 2017: scaled dot-product attention, multi-head attention, WMT 2014 BLEU benchmarks (Transformer big: 28.4 EN→DE, 41.8 EN→FR), and a critique noting quadratic sequence-length cost and reliance on positional encodings.

## Reading state

- **Attention Is All You Need** — status: **reviewed**. Rated 5/5, recommendation: must-read, reproducibility: code-only. Core takeaways: self-attention gives every token a direct path to every other token; the architecture improves parallelism over recurrence; positional encodings are required because attention alone has no built-in token order.

## Due for revisit

Nothing due. The gradient descent note is scheduled for revisit on **2026-08-03** — two days from now, so it will enter the window early next week.
